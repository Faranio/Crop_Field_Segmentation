default:
  redis:
    image: redis
    ports:
      - "{{infra.REDIS.port}}:6379"
    volumes:
      - /docker_data/volumes/redis_data:/data
    command: "redis-server --requirepass {{infra.REDIS.password}}"
    sysctls:
      net.core.somaxconn: "1024"
